# Log Analyzer V1

A Python-based log analyzer for detecting brute-force login attempts
in authentication logs.

## Features

- Parses Linux `auth.log` files
- Detects IP addresses with repeated failed attempts
- Exports a simple text report
- CLI-based interface

## Usage

Run the tool in your terminal:

```bash
python log.py
